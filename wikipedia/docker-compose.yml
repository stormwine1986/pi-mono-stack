services:
  wikipedia:
    build:
      context: .
      dockerfile: Dockerfile
      network: host
    container_name: wikipedia
    labels:
      role: Tools
    network_mode: host
    restart: unless-stopped
